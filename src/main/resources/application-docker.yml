spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/${DB_NAME:acme_saas}
  flyway:
    url: jdbc:postgresql://postgres:5432/${DB_NAME:acme_saas}
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: ${KEYCLOAK_ISSUER_URI:http://keycloak:8080/realms/acme}
